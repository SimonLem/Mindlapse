{
  "name": "@repo/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": "./dist/index.js" },
  "scripts": {
    "clean": "rimraf dist",
    "build": "pnpm -w exec tsc -p packages/types/tsconfig.build.json",
    "build:watch": "pnpm -w exec tsc -p packages/types/tsconfig.build.json -w"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
